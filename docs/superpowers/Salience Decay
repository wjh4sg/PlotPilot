仿生记忆算法空间
显著性衰减算法 (Salience Decay)
公式：S_now = S_initial * (1 - decay_rate) ^ ΔChapter
阈值规则：当 S_now < S_threshold 时，在下一次生成时排除出上下文。
默认参数（示例）：S_initial=100，S_threshold=50，decay_rate=0.04。
由参数推导阈值章数：ΔChapter_threshold = ceil( ln(S_threshold / S_initial) / ln(1 - decay_rate) )。
主角和核心主线实体的 decay_rate = 0（即不衰减）。
时间感知图谱 (Temporal Graph)
图数据库中的关系必须携带时间维。Cypher 检索逻辑：

MATCH (p:Person {name: "亚瑟"})-[r:HOLDS]->(i:Item {name: "圣剑"})
WHERE r.from_chapter <= $current_chapter 
  AND (r.to_chapter IS NULL OR r.to_chapter >= $current_chapter)
RETURN i.state
